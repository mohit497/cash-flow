version: '3.6'

services:
  backend:
    build:
      context: ./
      dockerfile: Dockerfile
    image: backend/latest
    restart: always
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
