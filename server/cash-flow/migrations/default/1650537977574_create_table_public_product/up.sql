CREATE TABLE "public"."product" ("id" uuid NOT NULL, "name" varchar NOT NULL, "code" varchar NOT NULL, "amount" numeric NOT NULL, "org" uuid NOT NULL, "createdAt" timestamptz NOT NULL DEFAULT now(), "updatedAt" timestamptz NOT NULL DEFAULT transaction_timestamp(), PRIMARY KEY ("id") , FOREIGN KEY ("org") REFERENCES "public"."org"("id") ON UPDATE cascade ON DELETE cascade);COMMENT ON TABLE "public"."product" IS E'holds product information';
