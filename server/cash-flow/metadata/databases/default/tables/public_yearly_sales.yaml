table:
  schema: public
  name: yearly_sales
select_permissions:
  - role: org-admin
    permission:
      columns:
        - yearly
        - total
        - shop
        - org
        - name
      filter:
        org:
          _eq: X-Hasura-Org-Id
      allow_aggregations: true
